#!/bin/bash

# This script is non-functional. Blazegraph creates a new KB (called namespace) automatically if the KB does not already exist.
# This script is required b/c it is called by the common build-from-scratch.sh script.