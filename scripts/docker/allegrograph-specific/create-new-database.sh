#!/bin/bash

# This script is non-functional. Allegrograph creates a new KB automatically if the KB does not already exist.
# This script is required b/c it is called by the common build-from-scratch.sh script.